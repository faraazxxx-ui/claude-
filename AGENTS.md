# claude-

Personal knowledge and working repository for Dr. Rahman.

When a run is launched from **Cursor for iOS** or cursor.com/agents on a
phone (`source: mobile` / `iosApp`), follow
`.cursor/skills/cursor-mobile/SKILL.md` before inventing process.

New Cursor skills belong in `.cursor/skills/<name>/SKILL.md` so Cloud
Agents and the iOS app actually load them. Duplicate into
`.claude/skills/<name>/` when Claude Code should see the same skill.
